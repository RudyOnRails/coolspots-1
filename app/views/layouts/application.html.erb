<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "CoolSpots" %></title>
    <%= csrf_meta_tags %>
    <%= stylesheet_link_tag "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
  </head>
  <body>

    <div class="navbar navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container">
          <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a>
          <a class="brand" href="/">CoolSpots</a>
          <div class="container nav-collapse">
            <ul class="nav">
              <li><%= link_to "Guides", guides_path %></li>
              <li><%= link_to "My Spots", spots_path %></li>
            </ul>
            <div class="pull-right">
              <% if logged_in? %>
              Welcome <%= current_user.username %> | <%= link_to 'sign out', logout_path %>
              <% else %>

              <% end %>
            </div>
          </div><!--/.nav-collapse -->
        </div>
      </div>
    </div>

    <div class="container" style="margin-top: 40px">
        <%= bootstrap_flash %>
        <%= yield %>
    </div> <!-- /container -->
    <footer style="padding: 20px; text-align: center">CoolSpots</footer>
  </body>
</html>
